import SwiftUI

enum TimerTheme {
    static let actionTint = Color(red: 0.85, green: 0.62, blue: 0.16)
    static let stopTint = Color(red: 0.72, green: 0.15, blue: 0.16)
    static let primaryText = Color(red: 0.95, green: 0.96, blue: 0.97)
    static let secondaryText = Color(red: 0.78, green: 0.82, blue: 0.86)
    static let backgroundTop = Color(red: 0.10, green: 0.12, blue: 0.15)
    static let backgroundBottom = Color(red: 0.20, green: 0.25, blue: 0.30)
}
